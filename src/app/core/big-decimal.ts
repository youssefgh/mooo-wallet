//import * as bigi from 'bigi';
//import * as BigDecimal from 'big.js';
import {Big} from 'big.js';

export class BigDecimalx {
/*
//    private 
    instance: Big;

    static fromNumber(n: number) {
        this.instance = new Big(n);    
    }
    
    private static fromBig(big: Big) {
        this.instance = new Big(n);    
    }

//    constructor(n: n    umber){
//        this.instance = new     Big(n);
//    }

//    static fromNumber(n: nu    mber) {
//        return BigInteger.fromBuffer(Buffer.from(n.toStri    ng()));
    //        }
//
//    static toNumber(bigInteger : BigInteger): n    umber {
//        return parseFloat(new Buffer(bigInteger.toHex(), 'hex').toStr    ing());
//        }

//    static test    (){
//        var n = 123.1    11;
//        var n2 =     10;
//        var b = bigi.fromBuffer(Buffer.from(n.toString()    ));
//        var b2 = bigi.fromBuffer(Buffer.from(n2.toString()    ));
//        b = b.min(b    2);
//        var nr = parseFloat(new Buffer(b.toHex(), 'hex').toString(    ));
//        console.log(n    r);
//    }

    static test() {
        var n = 899999.888888887;
        var n2 = 100000.11111        1111;


//        var n = 10000000.111        111111;
//        var n2 =       0.111181101;

        //        var n = 100000000000.111111111;
        //        var n2 = 101551510.111181101;
        var b = new BigDecimal(n);
        var b2 = new BigDecimal(n2);
        b = b.plus(b2);
        //        var nr: number = parseFloat(b.valueOf());
        //        console.log(nr);
    }

    plus(bigDecimal: BigDecimal): BigDecimal {
        let newBigDecimal = new BigDecimal(0);
        newBigDecimal. = this.instance.plus(bigDecimal.instance);
        return newBigDecimal;
    }

    minus(bigDecimal: BigDecimal): BigDecimal {
        return this.minus(bigDecimal);
    }

    toNumber() {
        return parseFloat(this.instance.valueOf());
    }
*/
}